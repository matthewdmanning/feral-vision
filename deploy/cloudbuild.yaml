# Configuration for the cloud . Operational steps live in
# scripts/cloud/ and consume these values through scripts/cloud/run.py.
defaults:
  - deploy_schema
  - _self_

substitutions:
  _GCP_PROJECT: cs-poc-kewg0kffb7uwobgq1rex2af
  _REGION: us-east4
  _REPO: feral-docker
  _BASE_IMAGE_NAME: feral-vision
  _IMAGE_NAME: feral-vision-gcp
  _IMAGE_TAG: default

options:
  logging: CLOUD_LOGGING_ONLY
