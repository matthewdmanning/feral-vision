# Run Recipes

Run Recipes are the only complete Hydra entrypoints. `baseline` is the
canonical local training recipe; `smoke` is CPU-safe for validation. Select a
different concern with a Hydra override, for example `model=yolo11n_seg`.
