# Local training dataset configuration (Hydra: data=base).
# All DataConfig fields explicitly set; override root via CLI: data.root=<path>

defaults:
  - base_data
  - _self_

source: local
root: data
image_size: 256
val_split: 0.2
