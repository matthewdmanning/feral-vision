# Inference configuration

Inference configuration selects tunable execution parameters for an inference
run. It does not define a prediction contract or reintroduce a predictor API;
those boundaries are owned by [ARCHITECTURE.md](../../ARCHITECTURE.md).

Choose a semantic inference variant as part of a complete run when inference is
wired through the canonical path.
