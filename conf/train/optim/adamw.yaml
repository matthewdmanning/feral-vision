defaults:
  - adamw_schema
  - _self_

# conf/train/optim/adamw.yaml
# AdamW — partial; call opt_factory(model.parameters()).
# Source: https://pytorch.org/docs/stable/optim.html

_target_: torch.optim.AdamW
_partial_: true
lr: 1e-3
betas: [0.9, 0.999]
eps: 1e-8
weight_decay: 1e-2
amsgrad: false
