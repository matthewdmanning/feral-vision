# conf/train/base.yaml

defaults:
  - base_train  # merges onto TrainConfig schema for struct-mode validation
  - optim: adamw
  - scheduler: cosine
  - loss_fn: cross_entropy

epochs: 50
batch_size: 32
num_workers: 0
device: cuda
