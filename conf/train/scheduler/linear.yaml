defaults:
  - linear_schema
  - _self_

# conf/train/scheduler/linear.yaml
# LinearLR — partial; call sched_factory(optimizer).
# Source: https://pytorch.org/docs/stable/generated/torch.optim.lr_scheduler.LinearLR.html

_target_: torch.optim.lr_scheduler.LinearLR
_partial_: true
start_factor: 0.3333333333333333
end_factor: 1.0
total_iters: 5
last_epoch: -1
