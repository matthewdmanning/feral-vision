defaults:
  - plateau_schema
  - _self_

# conf/train/scheduler/plateau.yaml
# ReduceLROnPlateau — partial; call sched_factory(optimizer).
# Source: https://pytorch.org/docs/stable/generated/torch.optim.lr_scheduler.ReduceLROnPlateau.html

_target_: torch.optim.lr_scheduler.ReduceLROnPlateau
_partial_: true
mode: min
factor: 0.1
patience: 10
threshold: 1e-4
threshold_mode: rel
cooldown: 0
min_lr: 0.0
eps: 1e-8
