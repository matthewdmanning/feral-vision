defaults:
  - l1_schema
  - _self_

# conf/train/loss_fn/l1.yaml
# L1Loss (MAE) — instantiated directly (no partial needed).
# Source: https://pytorch.org/docs/stable/generated/torch.nn.L1Loss.html

_target_: torch.nn.L1Loss
reduction: mean
