defaults:
  - bce_with_logits_schema
  - _self_

# conf/train/loss_fn/bce_with_logits.yaml
# BCEWithLogitsLoss — instantiated directly (no partial needed).
# Source: https://pytorch.org/docs/stable/generated/torch.nn.BCEWithLogitsLoss.html

_target_: torch.nn.BCEWithLogitsLoss
reduction: mean
