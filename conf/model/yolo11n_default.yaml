# Pretrained YOLO11n with its source-default eighty-class detector head intact.
defaults:
  - model_schema
  - _self_

architecture:
  source: ultralytics
  id: yolo11n.pt
  location: ultralytics.YOLO
  num_classes: 80

weights: null
